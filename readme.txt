before playing, be sure to write the command `pip install pyperclip` into your terminal.

Words are randomized daily and will not be the same as the official Wordle answer, but this project does use the same possible words as Wordle.